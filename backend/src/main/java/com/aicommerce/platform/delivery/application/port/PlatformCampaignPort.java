package com.aicommerce.platform.delivery.application.port; import java.util.UUID; import com.aicommerce.platform.delivery.domain.OperationOutcome; import com.aicommerce.platform.delivery.domain.ReconciliationResult; public interface PlatformCampaignPort { OperationOutcome submit(PlatformCommand command); ReconciliationResult reconcile(UUID operationUuid); PlatformDeliveryRecord readCampaign(UUID accountUuid,UUID campaignUuid); }
